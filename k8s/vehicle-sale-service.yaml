apiVersion: v1
kind: Service
metadata:
  name: vehicle-sale-service
  namespace: vehicle-system
spec:
  type: ClusterIP
  selector:
    app: vehicle-sale-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080